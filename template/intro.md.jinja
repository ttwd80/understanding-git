Introduction
===

This is another attempt to understand how git works.

As of `{{ git_version }}`, the git documentation page has {{ sections | length }} sections:

{%- for item in sections %}
{{ loop.index}}. {{ item }}
{%- endfor %}
