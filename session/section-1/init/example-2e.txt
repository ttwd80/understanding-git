mkdir ~/project
cd ~/project
git init .
git config core.filemode
cat .git/config
rm -rf .git
mkdir ~/base
echo "[core]" > ~/base/config
echo "filemode = false" >> ~/base/config
cat ~/base/config
git init --template ~/base
git status
git config core.filemode
cat .git/config
