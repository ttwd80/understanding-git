mkdir ~/project
cd ~/project
git init .
cat .git/config
rm -rf .git
mkdir ~/base
echo "something" > ~/base/content.txt
echo "<html>" > ~/base/config
ls -a
git init --template ~/base
git status
ls -a
ls -RF1 .git
cat .git/config
